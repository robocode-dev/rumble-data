# Architecture

<!-- clue:overview:bootstrap -->

Replace this bootstrap with a concise, evidence-based picture of the system before running `clue validate`. Cover the system's purpose and external actors, component and module boundaries, durable technology choices, and the links to architecture ADRs and capability designs. Use Mermaid when a context or component diagram makes those relationships easier to review. Do not repeat capability-local design here.

Architecture documents describe the system's shape, not individual features. A change updates this overview when it alters the system's structure or public surface; the decision behind such a change is recorded as an ADR in `decisions/` and links here when relevant.

<!-- clue:index:start -->
<!-- clue:index:end -->
